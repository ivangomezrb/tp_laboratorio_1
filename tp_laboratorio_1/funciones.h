/** \brief
  *Recibe dos numeros y los suma
  * \param primerNumero float
  * \param segundoNumero float
  * \return int
  *Devuelve la suma de ambos numeros.
  */
 int sumarDosNumeros(float,float);

  /** \brief
   *Recibe dos numeros y los resta
   * \param primerNumero float
   * \param segundoNumero float
   * \return int
   *Devuelve la resta de ambos numeros
   */
  int restarDosNumeros(float,float);

  /** \brief
   *Recibe dos numeros y los multiplica
   * \param primerNumero float
   * \param segundoNumero float
   * \return int
   *Devuelve la multiplicacion de ambos numeros
   */
  int multiplicarDosNumeros(float,float);

  /** \brief
   *La division de los dos numeros que el usuario ingresa.
   * \param primerNumero float
   * \param segundoNumero float
   * \return float
   *Devuelve la division de ambos numeros
   */
  float dividirDosNumeros(float,float);

  /** \brief
   *Recibe un numero y calcula su factorial
   * \param numero float
   * \return float
   *Retorna el factorial de dicho numero.
   */
  float calcularFactorial(float);
